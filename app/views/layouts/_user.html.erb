<div class="container" style="min-width:1170px;">

          <div id="google_translate_element" class="span1"></div>
            <script type="text/javascript">
              function googleTranslateElementInit() {
               new google.translate.TranslateElement({pageLanguage: 'es', layout: google.translate.TranslateElement.InlineLayout.HORIZONTAL, multilanguagePage: true}, 'google_translate_element');
              }
              </script><script type="text/javascript" src="//translate.google.com/translate_a/element.js?cb=googleTranslateElementInit">
            </script>


        <div class="span6">

<!--        <% if I18n.locale ==  :en %>
              <%= link_to 'Español', root_path(locale: 'es'), class: 'locale' %>
          <% else %>
              <%= link_to 'English', root_path(locale: 'en'), class: 'locale' %>
          <% end %>  --> 
        </div>
       
        
<div style="min-width:500px;" class="offset7">
          <%= render 'layouts/social' %>
          <div class="dropdown">
            <div class="btn-group">
              <button class="btn btn-warning dropdown-toggle" data-toggle="dropdown">
                <% if signed_in? %>
                    Menú
                <% else %>
                    Registrarse/Ingresar
                <% end %>
              </button>
              <ul class="dropdown-menu" role="menu">
                <div class="formulario-login">
                  <% if signed_in? %>
                      <%= render 'shared/signout' %>
                  <% else %>
                      <%= render 'shared/login' %>
                  <% end %>
                </div>
              </ul>
            </div>
          </div>

</div>
          
        
      </div>

  </div>
</div>